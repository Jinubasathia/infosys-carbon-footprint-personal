package com.infosys.carbonfootprint.service;
import com.infosys.carbonfootprint.dto.*; import org.springframework.web.multipart.MultipartFile; import java.util.*;
public interface ArticleService { List<ArticleDto> userArticles(); ArticleDto userArticle(Long id); List<ArticleDto> adminArticles(); ArticleDto save(ArticleRequest request,String author,Long id); ArticleDto saveWithImage(ArticleRequest request, MultipartFile image, String author, Long id); void delete(Long id); ArticleDto publish(Long id); ArticleDto unpublish(Long id); }